<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render "layouts/sidebar" %>
    </div>

    <div class="col-md-9">
      <% if @category == "movie" %>
        <%= render "movie_results", movies: @movies, content: @content %>
      <% else %>
        <%= render "tv_results", tvs: @tvs, content: @content %>
      <% end %>
    </div>
  </div>
</div>