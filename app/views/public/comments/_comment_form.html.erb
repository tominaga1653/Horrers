<%= form_with model: [post, comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '2', placeholder: "コメントはこちら", :required => true, class: "form-control comment-textarea" %>
  <%= f.submit "コメント", class: "btn btn-sm btn-secondary w-50 mt-2" %>
<% end %>